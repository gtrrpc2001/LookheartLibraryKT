package com.library.KTLibrary.myEnum

enum class DateType {
    DATE, TIME, HOUR, DATETIME
}

enum class LineChartType {
    BPM, HRV
}

enum class BarChartType {
    ARR, CALORIE, STEP
}

enum class ButtonType {
    TODAY, TWO_DAYS, THREE_DAYS
}

enum class TimePeriod {
    DAY, WEEK, MONTH, YEAR
}